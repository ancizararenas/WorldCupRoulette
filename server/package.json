{
  "name": "worldcup-roulette-api",
  "version": "2.0.0",
  "description": "REST API for La Chusma Mundealera 2026 — uses Node's built-in SQLite (no native build)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
